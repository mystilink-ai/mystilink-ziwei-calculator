---
description: 追求最广覆盖：多语言、多平台、多代码语言，引用即用
alwaysApply: true
---

# 覆盖面与可引用性

设计与实现默认追求覆盖面最大化，让用户「引用即用」，而不是绑定单一栈或单一环境。

## 原则

- 多自然语言：文档与面向用户的文案优先提供多语言（至少中英；有用户需求时扩展），文案键与文案文件分离
- 多运行平台：覆盖主流桌面/移动/服务端目标（如 Web、iOS、Android、macOS、Windows、Linux），避免写死单一 OS API
- 多代码语言：遵循 `language-matrix` 规则（C / C++ / C# / Java / JS·Node / Python）
- 引用即用：最小依赖、稳定公共 API、完整安装与最小可运行示例；默认配置可跑通，高级能力可选开启

## 实现要求

- 新增功能时评估：是否可在其他语言/平台复用同一契约（详见语言矩阵）
- 平台相关代码隔离在 adapter / platform 层，核心逻辑保持可移植
- 破坏性变更需有迁移说明；公共 API 变更保持向后兼容或提供并列版本路径
- 示例代码可复制运行，不依赖未公开的内部环境

```text
# ❌ BAD
仅单一平台可用；示例依赖内网服务且无占位配置。

# ✅ GOOD
核心契约稳定；多平台适配；按语言矩阵交付；README 含可本地跑通的最小示例。
```
