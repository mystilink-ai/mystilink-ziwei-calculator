# Python binding

Install the root package with `pip install -e .`, then:

```python
from mystilink_ziwei import ChartInput, compute_chart
```

Optional CLI parity helper: `ziwei_binding.chart_via_cli(...)`.
