---
description: 强制编程语言矩阵：C/C++/C#/Java/JS(Node)/Python 可引用交付
alwaysApply: true
---

# 语言矩阵

功能性仓库（SDK、库、服务客户端、可集成组件）必须按下列矩阵提供可引用交付，使用户在各语言中「引用即用」。纯文档仓、awesome 列表仓可豁免，但须在 README 标明不适用。

## 必选目标

| 目标 | 最低交付 |
|------|----------|
| C | 稳定 C API（头文件 + 库）或等价 FFI 边界 |
| C++ | C++ API，或对 C API 的薄封装 |
| C# | 可引用包（如 NuGet）或正式绑定 |
| Java | 可引用包（Maven/Gradle）或正式绑定 |
| JavaScript | npm 包；须同时支持 Browser 与 Node（同包双入口或明确 exports） |
| Python | 可安装包（如 PyPI）或正式绑定 |

JavaScript 与 Node 视为同一 npm 交付线，不得只支持其一。

## 「支持」判定（每种语言均须满足）

1. 可安装 / 可链接的交付物与版本说明
2. README（各语言版）含该语言的安装与最小用法
3. `examples/<lang>/` 下有可本地跑通的最小示例
4. 与核心契约一致（类型、错误码、行为映射清晰）

## 架构约定

- 契约优先：先定 C ABI、OpenAPI、proto 或 CLI 等稳定边界，再铺各语言绑定
- 推荐目录：`c/` 或 `include/`、`cpp/`、`bindings/{csharp,java,js,python}/`、`examples/{c,cpp,csharp,java,js,node,python}/`
- 新增公共能力须同步矩阵内全部绑定；不能同步时不得合入，或同变更内提供等价替代调用方式
- 若某语言无法提供原生 SDK，须提供矩阵内语言均可使用的替代（HTTP/CLI 等），并在 README 写明

## 禁止

- 仅单一语言实现功能，其余语言长期缺失或仅占位空目录
- JS 只做 Browser 或只做 Node
- 示例依赖未公开环境、真实密钥或内网地址

```text
# ❌ BAD
只有 Python 包；其他语言无绑定、无示例。

# ✅ GOOD
核心契约 + C/C++/C#/Java/JS(Node)/Python 均可引用；各语言 examples 可跑通。
```
