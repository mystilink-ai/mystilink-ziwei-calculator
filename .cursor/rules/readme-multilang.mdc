---
description: 每个仓库必须有详尽主 README，并做多语言管理与互链
alwaysApply: true
---

# README 与多语言

每个仓库必须提供详尽 README，并做多语言管理；内容只写功能与用法（见功能描述优先规则）。

## 结构

- 主 README：仓库根目录 `README.md`（默认语言，通常为英文或团队约定的主语言）
- 多语言副本：如同级 `README.zh-CN.md`、`README.ja.md` 等，或统一放在 `docs/readme/` 下并在根目录保留主 `README.md` 入口
- 语言集合至少覆盖：英文 + 简体中文；按用户群扩展其他语言
- 各语言 README 内容对等：章节结构一致，功能说明、安装、示例、配置、限制同步更新

## 开头互链（强制）

每个 README（含主 README）都必须在文件最开头提供其他语言版本的链接，用户一打开即可切换。

```markdown
# Project Name

> Languages: [English](README.md) | [简体中文](README.zh-CN.md) | [日本語](README.ja.md)

## Overview
...
```

## 详尽程度（最低要求）

主 README 与各语言版至少包含：

1. 项目用途与能力边界
2. 支持的平台 / 语言 / 运行环境
3. 安装与快速开始（可复制的最小示例）
4. 配置项与常用 API / 用法
5. 示例或教程入口
6. 许可声明入口（指向 `LICENSE`，不展开宣传叙事）
7. 贡献 / 问题反馈的必要技术说明（若适用）

## 维护

- 改功能或 API 时，同步更新所有语言 README，禁止只更新一种语言
- 新增语言时，补齐互链，并保证与主 README 结构对齐
