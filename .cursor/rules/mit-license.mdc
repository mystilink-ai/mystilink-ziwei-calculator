---
description: 每个仓库统一使用 MIT License
alwaysApply: true
---

# MIT License

每个仓库必须采用 MIT License，不得改用其他许可证，也不得在无 `LICENSE` 的情况下发布。

## 要求

- 仓库根目录必须有名为 `LICENSE`（或 `LICENSE.md`）的 MIT 许可全文
- 版权声明使用正确的版权年份与版权方名称
- 包元数据（如 `package.json` 的 `license`、PyPI、Cargo 等）声明为 `MIT`
- README 仅链接到 `LICENSE` 或标注 `MIT`，不展开协议宣传或与功能无关的许可叙事

## 禁止

- 使用 Apache / GPL / BSD 等其他许可证替代 MIT
- 在业务代码或文档中粘贴残缺、篡改后的许可文本
- 遗漏根目录 `LICENSE` 文件

```text
# ❌ BAD
无 LICENSE；或 license 字段为 ISC / Apache-2.0。

# ✅ GOOD
根目录 LICENSE 为标准 MIT 全文；元数据 license: MIT；README 指向 LICENSE。
```
