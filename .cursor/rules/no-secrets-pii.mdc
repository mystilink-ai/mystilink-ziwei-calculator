---
description: 禁止在仓库中暴露密钥、IP 地址与个人信息
alwaysApply: true
---

# 安全与隐私红线

本仓库面向公开分发。任何提交、文档、示例、配置、日志或注释都不得包含真实敏感信息。

## 禁止提交

- 密钥与凭证：API Key、Token、Password、私钥、证书口令、Webhook Secret、数据库连接串中的口令
- 网络地址：真实公网/内网 IP、未脱敏的内网主机名、VPN/跳板机地址
- 个人信息：真实姓名、手机号、邮箱、身份证号、住址、账号 ID、客户数据

## 正确做法

- 使用占位符：`YOUR_API_KEY`、`example.com`、`203.0.113.10`（文档用 IP）、`user@example.com`
- 敏感配置放在 `.env` / 本地私有文件，并确保已被 `.gitignore` 忽略
- 示例只展示结构与字段名，不写真实值
- 发现误提交时立即轮换凭证，并从历史中清除（勿仅靠后续 commit 覆盖）

```bash
# ❌ BAD
API_KEY=sk-live-abc123
HOST=10.12.34.56
CONTACT=zhangsan@company.com

# ✅ GOOD
API_KEY=YOUR_API_KEY
HOST=api.example.com
CONTACT=support@example.com
```
