# CLI notes

Install the Python package (`pip install -e .` from the repository root). The console script `mystilink-ziwei` is the stable contract used by language bindings.

Environment override for bindings:

```text
MYSTILINK_ZIWEI_CLI=/absolute/or/relative/path/to/mystilink-ziwei
```
